% tables/tab2-1-cannot-express.tex — 4 条在通用 IR 下不可直接等价验证的规则
\begin{table}[t]
\centering
\caption{在 Alive2（基于 LLVM IR）下因多返回值结构表达限制而无法直接建立等价性的 4 条典型进位链规则。前 3 条为单条 \texttt{ADC}/\texttt{SBB} 规则（右式丢弃进位输出），第 4 条为 4-limb 进位链合并规则。}
\label{tab:cannot-express}
\footnotesize
\begin{tabular}{@{}ll@{}}
\toprule
规则 & 重写形式 \\
\midrule
\texttt{adc\_zero\_carry}  & $\texttt{ADC}(x,y,0) \to \texttt{add}(x,y)$ \\
\texttt{sbb\_zero\_borrow} & $\texttt{SBB}(x,y,0) \to \texttt{sub}(x,y)$ \\
\texttt{sbb\_self\_zero}   & $\texttt{SBB}(x,x,0) \to 0$ \\
\midrule
4-limb ADC 链 & $4 \times \texttt{uadd.w.overflow} \to \texttt{add.i256}$ \\
\bottomrule
\end{tabular}
\end{table}
