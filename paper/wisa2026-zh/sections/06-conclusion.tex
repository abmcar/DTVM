% 06-conclusion.tex — §6 结论
\section{结论}

本文为确定性 EVM JIT 构建了两级窥孔重写系统：dMIR 层 70 条规则通过 Z3
位向量等价性校验，x86 层 13 条机械冗余清理规则由对照测试套件保障。与
Albert 等人 CAV 2023~\cite{verified_evm_cav23}的字节码块级规则集仅在通用
位向量恒等式上小幅交集，其余部分互补。本文方法论——在领域专用 IR 上引入
一等进位链原语以扩展自动等价验证的覆盖面——原则上适用于其他多字算术确定性
执行环境（WASM 大整数扩展、zk-VM 后端等）。
