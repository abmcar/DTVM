% 06-conclusion.tex — §6 结论
\section{结论}

本文为确定性 EVM JIT 构建两级窥孔重写系统：dMIR 层 70 条规则通过 Z3 位向量
等价性校验，x86 层 13 条由对照测试套件保障。与 Albert 等人 CAV
2023~\cite{verified_evm_cav23}的字节码块级规则集仅在通用位向量恒等式上小幅
交集，其余互补。本文方法论——在领域专用 IR 中引入一等进位链原语，以扩展
自动等价验证覆盖面——原则上适用于其他多字算术确定性执行环境
（WebAssembly 大整数扩展、零知识证明虚拟机（zk-VM）后端等）。
