% 02-problem.tex — §2 Background and motivation
\section{Background and Motivation}
\label{sec:problem}

\subsection{Hardware Cost and Carry-Chain Shape of U256 Arithmetic}

The native word length of the EVM is 256 bits, whereas general-purpose
registers on current commodity hardware are 64 bits wide. An EVM-level
U256 addition expands, after JIT code generation, into four machine-level
additions: one ordinary 64-bit \texttt{add} followed by three
carry-propagating \texttt{adc} instructions that form a \emph{serial
carry chain}. Subtraction is analogous, using one \texttt{sub} and three
\texttt{sbb} instructions. The listing below shows the 4-limb code on
both sides for a typical U256 addition:

\begin{center}
\small
\begin{tabular}{@{}l|l@{}}
\textbf{dMIR (4-limb addition)} & \textbf{x86-64 output} \\
\hline
\texttt{r0 = add(a0, b0)}   & \texttt{add rax, rcx}  \\
\texttt{r1 = ADC(a1, b1, cout0)} & \texttt{adc rbx, rdx}  \\
\texttt{r2 = ADC(a2, b2, cout1)} & \texttt{adc rsi, rdi}  \\
\texttt{r3 = ADC(a3, b3, cout2)} & \texttt{adc r8,\enspace r9} \\
\end{tabular}
\end{center}

Among all instructions emitted by the JIT, instructions directly tied to
U256 multi-word arithmetic account for about \textbf{2.15\%} (weighted
mean across the U256-heavy subset of the 27 benchmarks in
\S\ref{sec:evaluation}---the leftmost cluster ``U256-heavy (0--10)'' in
Figure~\ref{fig:speedup}). The share
is small, but the carry chain has a special property: every
\texttt{adc}/\texttt{sbb} on the chain depends on the carry output of the
previous instruction, forming a strict \emph{serial data dependency}
that instruction-level parallelism cannot hide.
Their serial dependencies still make them plausible optimization
targets; \S\ref{sec:perf} confirms the chain accounts for the bulk of
the +7.24\% geo-mean despite being 2.15\% of dynamic instructions.
Section~\ref{sec:perf} reports per-family hit rates: on U256-heavy
benchmarks the U256 rule hit rate ranges from 25\% to 99.9\% with a
very uneven distribution; on non-U256-heavy benchmarks (sha1 family,
\texttt{jump\_around}) it is well below that range, and gains come
from x86-layer mechanical cleanup.

\subsection{Carry-Chain Expressiveness Limits in General-Purpose IRs}
\label{sec:expressiveness}

{\sloppy
In LLVM IR, basic arithmetic (\texttt{add}, \texttt{sub}, \texttt{mul})
is defined only on $N$-bit integers \texttt{iN}; to obtain the carry
bit, the programmer must call
\texttt{@llvm.uadd.with.overflow.iN}, which returns a
\texttt{\{iN, i1\}} multi-return tuple. The carry is \emph{not a native
type} but hidden in the second field of a struct. Following the
Alive/Alive2~\cite{alive,alive2} peephole verification framework,
Table~\ref{tab:cannot-express} lists four carry-chain rules that
Alive2 cannot directly verify (type mismatch on the multi-return
tuple, or timeout on the lifted encoding); the limitation stems from
IR carry modeling, independent of solver capacity.\par}

\input{tables/tab2-1-cannot-express}

The four rules' Alive2 command lines, failure messages, and tool
versions (Alive2 v21.0, Z3 4.8.12) are archived in the artifact
(\S\ref{sec:availability}).

The bottleneck is not the SMT solver~\cite{demoura2008z3}: the six
U256 rules without multi-return carry pass Alive2 in $\sim$60\,ms each.

{\sloppy
In LLVM IR, expressing one carry-propagating addition takes five lines
around \texttt{uadd.with.overflow}; in dMIR, a single \texttt{ADC}
suffices. Native carry-bit support brings all four rules into the scope
of automatic equivalence checking.\par}
