% figures/fig1-architecture.tex — two-tier system overview
\begin{figure}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\footnotesize},
  box/.style={draw, rounded corners=2pt, inner sep=4pt, minimum height=9mm, minimum width=18mm, align=center, fill=gray!5},
  smallbox/.style={draw, rounded corners=2pt, inner sep=3pt, minimum height=8mm, minimum width=18mm, align=center, fill=gray!5},
  arrow/.style={-{Latex[length=2mm]}, semithick},
  region/.style={font=\small\bfseries},
  divider/.style={dashed, gray!60},
]

% --- Offline cluster (left) ---
\node[box]      (rules)    at (0,0.0)   {Production\\rule set\\\scriptsize{(70 dMIR + 13 x86)}};
\node[smallbox] (z3)       at (3.0,0.8) {Z3\\admission};
\node[smallbox] (enum)     at (3.0,-0.8){Canonical-LHS\\enumeration};
\node[box]      (compiled) at (6.0,0.0) {Compiled\\rule set};

% --- Runtime cluster (middle-right) ---
\node[smallbox] (dmir)     at (9.0,0.8) {dMIR\\matcher};
\node[smallbox] (x86)      at (9.0,-0.8){x86 CgIR\\cleanup};

% --- Correctness gate (right) ---
\node[box]      (diff)     at (12.2,0.0){5{,}884 differential\\Cancun tests};

% Region labels
\node[region] at (1.5, 2.0)  {Offline};
\node[region] at (9.0, 2.0)  {JIT runtime};
\node[region] at (12.2, 2.0) {Correctness gate};

% Vertical dashed dividers
\draw[divider] (4.5,-2.0) -- (4.5,1.7);
\draw[divider] (10.6,-2.0) -- (10.6,1.7);

% Edges
\draw[arrow] (rules.east) -- (z3.west);
\draw[arrow] (rules.east) -- (enum.west);
\draw[arrow] (z3.east) -- (compiled.west);
\draw[arrow] (enum.east) -- (compiled.west);
\draw[arrow] (compiled.east) -- (dmir.west);
\draw[arrow] (compiled.east) -- (x86.west);
\draw[arrow] (dmir.east) -- (diff.west);
\draw[arrow] (x86.east) -- (diff.west);

\end{tikzpicture}%
}
\caption{Two-tier system overview. The offline pipeline submits the
production rule set to Z3 for admission and audits its coverage via
canonical-LHS enumeration; the resulting compiled rule set drives the
JIT, where the dMIR matcher applies algebraic rewrites and the x86
CgIR layer applies hardware-specific cleanup. Correctness is gated by
the matched 5{,}884-test differential suite.}
\label{fig:architecture}
\end{figure}
